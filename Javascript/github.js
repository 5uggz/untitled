// Redirect the user to a new URL
function redirectToLink() {
    window.location.href = "https://www.example.com"; // Replace later with github link
}

// Call the function to trigger the redirection
redirectToLink();
